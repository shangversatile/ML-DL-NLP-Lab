seed: 42

data:
  n_samples: 100
  n_features: 2
  noise: 0.1
  val_ratio: 0.2

training:
  learning_rate: 0.05
  num_epochs: 200
  log_every: 20

logging:
  log_file: results/logs/linear_regression_training.log

output:
  loss_curve_path: results/figures/linear_regression_loss_curve.png
